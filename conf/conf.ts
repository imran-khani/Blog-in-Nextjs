
export const conf = {
    sanityProjectId: String(process.env.NEXT_PUBLIC_SANITY_PROJECT_ID),
    sanityDataset: String(process.env.NEXT_PUBLIC_SANITY_DATASET),
    sanityApiVersion: String(process.env.NEXT_PUBLIC_SANITY_API_VERSION),
}